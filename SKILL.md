---
name: interview-guide-builder
description: 为任意项目搭建"面试指导书工作区"并逐章成书：定界 → 建工作区（规则入口/事实边界/事实包/执行手册）→ 黄金样例定稿 → 一章一任务写作与验收 → 产出不合意时诊断规格。章节产物为双层结构：口述层（五段模板，可直接口述）+ 支撑层（机制详解/简历↔实现对照/公平对比/高概率追问）。Use this skill whenever the user wants to 面试讲项目、写面试指导书、准备项目面试口述稿、把某项目整理成面试材料、build an interview guide for a project，或要继续/验收/诊断已有的 interview-guide-workbench 工作区——即使用户没有明确说"指导书"三个字。
---

# 面试指导书工作区：搭建、成书与诊断

本 skill 沉淀自 akashic-agent 项目的完整实践（2026-09 重建版工作区）。可复用的不是该项目的事实内容，而是五条思考方式（§1）与由它们落成的规格标准、工作区结构和极简流程。项目特定内容（章节分解、数字、符号）在每个新项目中重新生成。

**适用边界**：本 skill 为"面试讲解"设计——五段模板、"能否直接口述"验收都锚定这个目标。用户要的是 onboarding 文档、设计文档等非面试产物时，标准不适用，先与用户确认目标再动工。

## 1. 核心思考方式（优先级高于一切条款）

1. **规格先行：产出不合意，先怀疑规格，再怀疑执行。** 产出反复不达预期时，最常见的根因是产物从未被定义清楚——执行 Agent 忠实执行了错误规格。在要求"写得更好"之前，先检查目标产物定义是否存在、是否错位。对任务理解有歧义时不推进，先问。

2. **深度问题用分层解决，不用禁令解决。** "不够深"→加规则→"过深"→再加禁令，这个摆动不会收敛，因为两层需求被当成了同一件事。解法是给深度定义归宿：口述层白话可口述（符号几乎不出现），支撑层深挖（符号可以出现但必须随文解释）。深度标尺回答"哪个深度的内容、以什么形式、放在产物的哪个位置"。

3. **规则靠正向样例锚定，不靠禁止项堆叠。** 每次返修追加一条"不许 X"，规则只增不减、越来越防御，产物反而离目标更远——因为规则从未告诉执行者"好的长什么样"。一段经用户逐句认可的黄金样例是全书规格的锚点，比几十条禁令有效。样例先于章节定稿。

4. **产物规格刚性，思考路径弹性。** 两层结构、符号与链接规则、数字口径、可口答性必须执行（防深浅漂移的根基）；五段模板是内容完备性参考而非顺序铁律——叙事顺序与详略由执笔者按章自主决定，未解决的真实问题允许如实收尾（写清下一步与剩余不确定性），不虚构误判或修复闭环。研究顺序、结构细节同样自主。参考线索只是起点，可以推翻。批判不被压制而是分级出口：关键事实冲突先问再动笔；规则与风格异议随稿上交由用户裁决；执行细节自主。用户必须逐条裁决随稿议题——无声忽略会让批判通道名存实亡，执行者会学会沉默。

5. **流程服务于产出，仪式是负债。** 一章一任务、用户在章节颗粒度验收，就够了。阶段任务契约、YAML 状态机、跨文件状态同步解决的是"交接不丢信息"，不是质量问题，只会拉长修改周期。定点返修，不整篇重写。

事实纪律压缩清单（结构保留，不展开为哲学）：

- 事实取自事实包与钉死的版本基线；与简历或已确认结果冲突时交用户裁决，不编造数字、经历和事故。
- 数字必须带口径与限定语；没有效果数字就讲验证方式与边界，不硬凑。
- 维护禁用结论清单（不得无依据声称的表述）；材料未提及 ≠ 功能未实现。

## 2. 路由：先判断项目处于哪一步

1. 项目内不存在面试指导书工作区（默认路径 `<项目根>/docs/interview-guide-workbench/`，找不到时可全局搜同名目录或含"面试指导书"的 00-README）→ 从 §3 定界开始。
2. 工作区已存在 → 读其 `00-README.md`（唯一规则入口）与进度表，按 §5 续写。工作区自己的规则优先于本 skill 的通用表述。
3. 章节产出反复不合意、用户反馈在"太深/太浅/不对味"间摆动 → 进入 §6 诊断，不要直接再写一稿。

## 3. 阶段 0：定界（建区前与用户确认，产出章节分解表）

逐项确认，不确定就问：

- **面试场景**：岗位方向、时长（决定口述层篇幅预算）、面试官背景。
- **面试风格画像**：是否收集到目标方向的真实面试题/面经；有则归纳提问 archetype（如机制原理、配置数字、数据构造与筛选策略、流程分解、系统性能、决策动机、故障处理），作为支撑层追问板块的取材输入与验收抽查依据。画像显示深挖题主导（机制/数字/估算为主、无叙事题）时，向用户建议深挖型口述层模式（规格见 workbench-readme 模板 §3）。
- **讲解主线**：简历或能力清单在哪里（通常 `章 = 简历能力条目`，一条一章）；是否有公共技术地基（如统一运行时——它是各章共同基础，不另立成章）；同一链路是否存在多个算法/方案变体——若有，链路与变体分章，变体章只讲 delta 与演进动机（前序方法的不足如何催生新变体、回退关系是什么），不重复铺陈共享链路；未达预期的实验按"现象→归因→改善方向"组织，是高价值素材而非需要回避的瑕疵；总述章与对比附录按需。
- **事实来源**：版本基线（钉死 commit，防不同版本事实混流；多仓项目逐仓钉基线并声明用途，数据目录多版本时显式声明使用版本）；简历文件路径；可用的既有成体系文档（优先整合不重写，整合方式见 fact-packet 模板）。
- **产物去向**：工作区内成书即可，是否替换正式文档由用户全书完成后决定。

产出：章节分解表（章名 / 对应简历条目 / 事实来源 / 编写顺序建议），交用户确认后才进入搭建。

## 4. 阶段 1：搭建工作区

目录与文件名保持一致（后续会话和本 skill 靠它路由）：

```
docs/interview-guide-workbench/
  00-README.md               规则入口（唯一）
  01-fact-boundaries.md      事实边界：术语、数字口径、禁用结论、比较基准
  02-execution-playbook.md   执行手册：启动 prompt、验收要点、返修模板
  packets/NN-slug.md         每章一个事实包
  drafts/chapters/           章节产物
  drafts/archive/            旧稿与素材（降级存放，不继承结构与结论）
```

步骤与要点：

1. **黄金样例先行**（最重要的一步，先于任何章节）。读 `references/golden-sample-reference.md`（含 akashic 样例原文与起草方法），用本项目素材起草：一段口述层节选 + 一两条支撑层符号条目 + 链接格式示例。交用户**逐句认可**（"语感不对"是有效否决理由，改到认可为止）后嵌入 00-README §5。这一步验证的是整个规格是否命中用户预期——样例认可了，规格才算定稿。
2. **从模板生成四件套**：`templates/` 下四个模板，填 `{{占位符}}`。
   - `workbench-readme.md`：产物定义、五段模板、硬性标准、黄金样例、极简流程、进度表。规则只在这一份里说，别的文件不重复写作规则。
   - `fact-boundaries.md`：统一术语表（术语/含义/不应写成）、可用数字及限定语、禁用结论清单、版本基线、技术比较基准。多章共享的公共事实（如测评与 reward 链路、算法实际路径、实验身份与证据边界）单独成节、先于打样冻结——章节依赖的是其他章的事实而非成稿。
   - `fact-packet.md`：每章事实底稿。**只放事实，不放写作指令**——指令混进事实包会以旧标准指挥后续写作。覆盖：简历条目、关键事实（带来源与口径）、既有文档整合表（项目已有成体系文档优先整合进支撑层、不追求重写；逐条标注来源文档与版本，方式为原文取用或压缩改写）、数据、踩坑记录（现象→误判→根因→修复→验证）、边界与禁用推导、未决问题。
   - `execution-playbook.md`：每章一份启动 prompt（模板见其 §3）、通用验收要点、定点返修模板、定稿指令、常见偏差速查。
3. **打样章**：选素材最全的能力章先写第一站，立即按 §5 走完一轮验收——规格没命中预期就改规格（改 00-README 与样例），命中了再铺开其余各章。
4. **卫生**：旧稿移入 archive 并去掉误导性状态后缀；不留执行残骸（临时脚本、日志、整仓 clone）；规则文件保持单一入口、无双份。

## 5. 阶段 2：逐章成书（一章一任务循环）

每章循环（启动 prompt 从 playbook 取，规则不内联进 prompt，让执行者读文件——单一事实源）：

1. 执行者读 00-README + 01-fact-boundaries + 对应 packet（archive 素材可选，只取事实）；
2. 自主研究、取舍、写稿（packet 不足时读基线源码核实并记录所读文件）；
3. 交稿附两部分自查：**A 合规自查**（五段齐全、字数与符号数、符号随文解释、链接位置、数字口径与事实包一致）；**B 批判性自查与议题**（本章最薄弱处、对规则/样例/事实包的异议与建议、开放问题——不阻塞成稿，交用户裁决）；
4. 用户验收：**试讲口述层**（能否直接讲出来；计时按 00-README §3 所选模式——叙述型 3～6 分钟、深挖型 1.5～2 分钟）；支撑层随机抽 3 个符号，说不出作用即返修；数字回链事实包；逐条裁决随稿议题（成立就改规则并告知，不成立回复理由）；
5. 有问题 → 定点返修（playbook 模板：只改列出的问题，不整篇重写；返修点与其他内容冲突时先停下说明）；通过 → 定稿指令更新进度表。

编写顺序：能力章先行（打样章第一站），总述章与对比附录最后——它们直接取材于能力章成稿，顺序不可倒置。

章节产物规格（硬性标准，全文以工作区 00-README 为准，此处为路由与验收用的摘要）：

- **口述层**：第一人称叙事，默认按五段骨架组织——①背景与问题（卡在哪，现象与代价，不先堆架构）②我的判断（对比过什么、放弃了什么、依据）③具体行动（因果串成行动序列，不是功能清单）④结果与复盘（有数字讲数字并说明口径，没数字讲验证方式与边界）⑤取舍与踩坑（已闭环的按"现象→误判→根因→修复→验证"讲，未闭环的按"现象→判断→尝试→观察→下一步"如实收尾）。五段为内容完备性参考，顺序与详略按章自主。篇幅按模式：叙述型约 1000～1600 字；深挖型压缩导语约 400～600 字（完整讲稿只保留在总述章）。代码符号每章至多 3 个且首现括注作用；无链接、无 commit 号；不用生活化比喻。
- **支撑层**：四板块——关键机制详解（每个符号首现紧跟一句作用说明，无解释即违规）/ 简历效果↔实现技术对照表（简历每句主张一行）/ 与主流替代方案对比（对方以最强形态出场、覆盖方法谱系而非二元对照、说清本项目代价与对方更优场景）/ 高概率追问与要点（给方向不写逐字稿）。链接只出现在支撑层。读者读完任一条目能说出"这个名字是干嘛的"；审计过程等内部工作内容不进正文，留在事实包。

## 6. 阶段 3：诊断（产出反复不合意时）

先定界再动手：**问题在规格还是在执行？** 判据——执行者是否忠实执行了明文规格。忠实执行了而产出不对 = 规格错了，要求执行者"违反明文规则写得更合适"只会更糟。逐层排查：

| 层 | 排查什么 | 典型信号 |
| --- | --- | --- |
| 目标定义层 | 产物是否被定义成另一个东西 | 产物是审计式专著/学习文档，不是"可口述的讲解" |
| 深度标尺层 | 深度是否有分层归宿 | 在"函数名堆砌"与"机制讲不透"之间摆动 |
| 规则体系层 | 规则如何生长 | 条款是历次返修的化石；同一规则多处出现且冲突；**没有正向样例** |
| 流程层 | 仪式是否重于产出 | 状态同步、契约校验占掉交互大头，质量问题一个没出在交接上 |

处置原则：

- **重建规格，不续修条款**。继续打补丁的循环是：产物不满意→追加禁止项→规则更防御→产物离目标更远。规则只增不减而目标始终缺席，就是该重建的信号。
- 重建 = 一份新的短规则入口（产物定义 + 五段模板 + 深度标尺 + 黄金样例）替换旧的叠加规则；事实包等真实资产保留；旧稿降级为素材归档。
- 结构性变更先出诊断与方案交用户确认；删除类操作放最后集中执行；对既有文件的批量修改先给 diff 再落盘。

## 7. 附件索引

- `templates/workbench-readme.md` — 工作区规则入口模板（阶段 1 生成 00-README 用）
- `templates/fact-boundaries.md` — 事实边界模板（生成 01 用）
- `templates/fact-packet.md` — 事实包模板（生成 packets/ 用）
- `templates/execution-playbook.md` — 执行手册模板（生成 02 用，含启动 prompt/验收/返修模板）
- `references/golden-sample-reference.md` — akashic 黄金样例原文（标注来源，仅示范风格）与新项目样例起草方法。阶段 1 第 1 步必读。
