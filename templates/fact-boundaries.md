# 主张边界：术语、数字口径与禁用结论

> {{日期}} 创建。本文件供写作与自查使用；章节事实在各 `packets/` 中，不在本文件重复。写作规则以 `00-README.md` 为准。本文件不是面试答案模板。

## 1. 统一术语

每个易混术语一行：固定"本文含义"与"不应写成"，防止跨章表述漂移。示例行（来自 akashic 项目，填完后删除）：

| 术语 | 本文含义 | 不应写成 |
|---|---|---|
| {{术语}} | {{一句话含义，含边界}} | {{常见误写/弱化写法}} |
| 当轮解锁（示例） | 当前执行回合内使命中工具进入后续模型请求的受限集合，仍受容量约束 | 只返回工具名称；永久修改全局权限 |

## 2. 可直接使用的结果表述

数字类结论只有进入本节才可在正文使用，每句自带口径。示例（来自 akashic，填完后删除）：

- "{{指标}}在{{口径}}下为 {{数字}}。"
- "{{验证实验}}中，{{数字}}，得到 {{结果}}。"

并注明哪些表述属于闭环事实/一致性验证，不能转写为用户效果。

## 3. 必须带限定语的表述

| 想表达的内容 | 必须带的限定 |
|---|---|
| {{指标/参数}} | {{如"历史实现采用的参数"/"在 XX 局部工作负载中"}} |

## 4. 不得无依据使用的结论

- {{效果类断言，如"XX 准确率显著提升"、"XX 降低了 X%"——无数据支持不得写}}
- {{拼接类断言，如"历史成绩与当前成绩可以合并"}}

效果数字只有取得支持对应主张的数据、核对口径并经确认后才能使用。本清单是编写者检查项，正文无需逐条复述"不能说"。

## 5. 版本与基线

- 主仓库基线固定 `{{commit hash}}`{{（源码副本 `{{路径}}`）}}；不混入其他版本的结构与表述。
- {{相关仓库/插件规则：只读与基线对应或兼容的实现并记录版本；版本页面不能单独证明基线已具备某能力}}
- {{历史成绩/历史参数：仅作内部核查线索，不与最终结果合并}}

## 6. 技术比较基准（支撑层"替代方案对比"的起点）

- {{对比组 1：本项目选择 vs 主流方法谱系，要求对方以最强形态出场}}
- {{对比组 2……}}
- 一手参考：{{官方文档链接}}；它们解释替代技术，不证明本项目已采用。

## 7. 冲突处理

- 证据与简历、既定基线或已确认结果冲突时：先定位冲突并交用户决策，不擅自改主张、不拼接版本、不按旧稿措辞强行解释代码。
- 材料未提及不等于功能未实现。列"待补"前先分清：实现已存在 / 数据已采集未整理 / 统计未做 / 确缺功能。
