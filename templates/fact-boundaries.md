# 主张边界：术语、数字口径与禁用结论

> {{日期}} 创建。本文件供写作与自查使用；章节事实在各 `packets/` 中。写作规则以 `00-README.md` 为准。本文件不是面试答案模板。简历约束**成果主张**，不约束理解深度。

## 1. 统一术语

每个易混术语一行：固定"本文含义"与"不应写成"，防止跨稿表述漂移。示例行（来自 akashic 项目，填完后删除）：

| 术语 | 本文含义 | 不应写成 |
|---|---|---|
| {{术语}} | {{一句话含义，含边界}} | {{常见误写/弱化写法}} |
| 当轮解锁（示例） | 当前执行回合内使命中工具进入后续模型请求的受限集合，仍受容量约束 | 只返回工具名称；永久修改全局权限 |

## 2. 可直接使用的结果表述（按方法/阶段分块）

数字类结论只有进入本节才可在正文使用，每句自带口径；多方法/多阶段项目按块分开（各自的成果、配置与事件不混写）。示例（来自 akashic，填完后删除）：

- "{{指标}}在{{口径}}下为 {{数字}}。"
- "{{验证实验}}中，{{数字}}，得到 {{结果}}。"

并注明哪些表述属于闭环事实/一致性验证，不能转写为用户效果。

## 3. 数字三档（掌握／会估算／查阅即可）

- **掌握**（可口答）：{{设备与分配、模型/序列规模、关键并行选择、已确认结果……}}
- **会估算**（讲量级与变化原因）：{{权重、训练状态、激活、logits、KV cache……}}
- **查阅即可**（不背）：{{事故现场小数、完整超参、每轮耗时、提交号……}}

## 4. 必须带限定语的表述

| 想表达的内容 | 必须带的限定 |
|---|---|
| {{指标/参数}} | {{如"历史实现采用的参数"/"论文消融限于 XX 任务，不能平移"}} |

## 5. 不得无依据使用的结论

- {{效果类断言——无数据支持不得写；单项改动收益不得脱离"同期多项变化"限定}}
- {{归属类断言——如"已完成课程训练""X 导致 Y"，证据不足时降为候选解释}}
- {{材料整理工作（纠错/甄别/审计）不作为研发经历讲述}}

效果数字只有取得支持对应主张的数据、核对口径并经确认后才能使用。本清单是编写者检查项，正文无需逐条复述"不能说"。

## 6. 版本与基线

多仓项目逐仓钉死（单仓退化为表的第一行）：

| 仓/数据目录 | 分支/版本 | commit | 用途（为哪些问题提供什么事实） | 读取方式 |
|---|---|---|---|---|
| {{仓路径}} | {{分支}} | {{commit hash}} | {{……}} | {{本地检出 / `git show <ref>:<path>` 只读}} |

- 不混入基线之外版本的结构与表述；数据目录多版本时声明使用版本，其余仅作历史线索。
- {{相关规则：只读与基线对应的实现并记录版本；版本页面不能单独证明基线已具备某能力}}

## 7. 面试风格画像（有真题语料时保留本节）

- 提问 archetype 归纳：{{……}}
- **真题转换原则**：原题→考察倾向→本项目对应问题；原题仅存档追溯，不按题数分配篇幅，题面内容本项目未涉及的舍弃、不计为缺口。
- 转换后的重点追问清单：{{全书共用取材}}

## 8. 技术比较基准（支撑内容比较板块的起点；总表归宿在附录）

- {{对比组 1：本项目选择 vs 主流方法谱系，对方以最强形态出场}}
- 一手参考：{{官方文档链接}}；它们解释替代技术，不证明本项目已采用。

## 9. 公共事实底稿（先于打样冻结；跨问题共用口径与机制）

{{测评/信号链路、方法实现口径、实验身份与证据边界等跨问题公共事实，逐条带来源与核验状态；问题稿依赖事实而非彼此成稿}}

## 10. 冲突处理

- 证据与简历、既定基线或已确认结果冲突，或对规则/结构有异议：**先停下问用户，裁决后再动笔**（阻塞式），不擅自改主张、不拼接版本。
- 材料未提及不等于功能未实现。列"待补"前先分清：实现已存在 / 数据已采集未整理 / 统计未做 / 确缺功能。
